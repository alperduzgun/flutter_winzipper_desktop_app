name: winzipper
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=2.19.6 <3.0.0'

dependencies:
  bitsdojo_window: ^0.1.5
  file_selector_macos: ^0.9.0+8
  flutter_acrylic: ^1.1.2
  macos_window_utils: ^1.8.1  # Updated for Ventura/Sonoma compatibility
  archive: ^3.6.1  # Latest stable with memory improvements
  file_picker: ^6.1.1
  path: ^1.8.3
  path_provider: ^2.1.2
  http: ^1.1.0  # Cloud upload
  flutter_bloc: ^8.1.3  # State management with Cubit
  qr_flutter: ^4.1.0  # QR code generation
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
